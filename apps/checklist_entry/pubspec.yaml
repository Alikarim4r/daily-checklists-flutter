name: checklist_entry
description: MOEHE Facility Daily Inspection — technician entry app.
publish_to: "none"
version: 1.2.1+6

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  checklist_shared:
    path: ../../packages/checklist_shared
  flutter_riverpod: ^2.6.1
  intl: ^0.20.2
  image_picker: ^1.1.2
  signature: ^5.5.0
  shared_preferences: ^2.5.3
  connectivity_plus: ^7.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: assets/branding/app_icon_simple.png
  adaptive_icon_background: "#0E6B6A"
  adaptive_icon_foreground: assets/branding/app_icon_simple.png
  macos:
    generate: true
    image_path: assets/branding/app_icon_simple.png
  web:
    generate: true
    image_path: assets/branding/app_icon_simple.png
    background_color: "#0E6B6A"
    theme_color: "#14919B"

flutter:
  uses-material-design: true
  assets:
    - assets/branding/app_icon_simple.png
